[package]
edition = "2021"
name = "peppermint-proto"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
prost = "0.9"
tonic = "0.6"

[build-dependencies]
tonic-build = "0.6"
